label= t('sidebar.radius_description')
  | &nbsp;
  select#radius(name="radius")
    each value in values
      option(value=value, selected = value==selected ? "" : null) 
        = humanDistance(value)
  | &nbsp;
  span#close-radius.icon.icon-icon-close
